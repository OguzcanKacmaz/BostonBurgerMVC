@model List<Atistirmalik>

@if (Model != null && Model.Count > 8)
{
    <div class="dropdown">
        <button class="dropbtn">Atıştırmalık</button>
        <div class="dropdown-content">
            <div class="d-flex">
                <div>
                    @foreach (var atistirmalik in Model)
                    {
                        if (atistirmalik.ID % 2 == 1)
                        {
                            <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@atistirmalik.ID" asp-route-tur="Atistirmalik" class="btn">@atistirmalik.Adı</a>
                        }
                    }
                </div>
                <div>
                    @foreach (var atistirmalik in Model)
                    {
                        if (atistirmalik.ID % 2 == 0)
                        {
                            <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@atistirmalik.ID" asp-route-tur="Atistirmalik" class="btn">@atistirmalik.Adı</a>
                        }
                    }
                </div>
            </div>
        </div>
    </div>
}
@if (Model != null && Model.Count <= 8)
{
    <div class="dropdown">
        <button class="dropbtn">Atıştırmalık</button>
        <div class="dropdown-content">
            @foreach (var atistirmalik in Model)
            {
                <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@atistirmalik.ID" asp-route-tur="Atistirmalik" class="btn">@atistirmalik.Adı</a>
            }
        </div>
    </div>

}
