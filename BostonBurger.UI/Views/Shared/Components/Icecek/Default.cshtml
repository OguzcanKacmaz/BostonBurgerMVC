@model List<Icecek>

@if (Model != null && Model.Count > 8)
{
    <div class="dropdown">
        <button class="dropbtn">İçecekler</button>
        <div class="dropdown-content">
            <div class="d-flex">
                <div>
                    @foreach (var icecek in Model)
                    {
                        if (icecek.ID % 2 == 1)
                        {
                            <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@icecek.ID" asp-route-tur="Icecek" class="btn">@icecek.Adı</a>
                        }
                    }
                </div>
                <div>
                    @foreach (var icecek in Model)
                    {
                        if (icecek.ID % 2 == 0)
                        {
                            <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@icecek.ID" asp-route-tur="Icecek" class="btn">@icecek.Adı</a>
                        }
                    }
                </div>
            </div>
        </div>
    </div>
}
@if (Model != null && Model.Count <= 8)
{
    <div class="dropdown">
        <button class="dropbtn">İçecekler</button>
        <div class="dropdown-content">
            @foreach (var icecek in Model)
            {
                <a asp-action="Index" asp-controller="UrunGoster" asp-route-urunNo="@icecek.ID" asp-route-tur="Icecek" class="btn">@icecek.Adı</a>
            }
        </div>
    </div>

}
